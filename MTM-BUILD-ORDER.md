# MTM — ترتيب التنفيذ: هذه النقطة أولا، ثم هذه

**التاريخ:** 25 أغسطس 2026 · **مُساند لـ** `MTM-Unified-Roadmap.md` **Rev 4.0** و`close-out/10-PHASE-ENTRY-PLAN.md`

---

## ما هذا الملف، وما ليس هو

**هو:** ترتيب واحد للتنفيذ، من أعلى لأسفل، بلا تفرّع. كل خطوة تنتهي على **فحص يمرّ أو يفشل** — لا على رأي بأن العمل «انتهى».

**ليس هو:** مصدر حجّية. الحجّية عند `close-out/12` ثم `01` ثم الخارطة ثم `10` (السلّم كامل في `CLAUDE.md` §1). **فإن خالف هذا الملف أيّها، فالحقّ لها وهذا الملف هو الذي يُصحَّح.** سببه أنه مشتقّ منها، والمشتقّ لا يعلو على أصله.

**ولماذا يوجد أصلا:** `10` يصف المراحل وشروط خروجها، ولا يقول **ما يُلمس أوّلا داخل المرحلة**. هذا الملف يقول ذلك، ولا يضيف قرارا جديدا واحدا.

---

# الجزء أ — ما أُنجز فعلا (25 أغسطس 2026)

| # | العمل | الحالة | الأثر |
|---|---|---|---|
| أ‑1 | المسار بلا مسافات: `…/apps/medical_pro/mtm_app` | ✅ | مسافة واحدة في المسار تكسر Gradle/NDK/CocoaPods، و`MTM app` ليس اسم حزمة Dart صالحا |
| أ‑2 | `git init` + لقطات متتالية + شجرة نظيفة | ✅ | قبلها كان أي كتابة فوقية **غير قابلة للاستعادة** |
| أ‑3 | `.gitignore` بقسم أسرار صريح | ✅ | كل مفتاح في `close-out/09` **يُحقَن ولا يُودَع** |
| أ‑4 | `CLAUDE.md` في الجذر | ✅ | ترتيب القراءة + **تسعة** قيود + السبعة غير الحجّية (أربعة مُبطلة + ثلاثة برومتات) |
| أ‑5 | **مصدر حقيقة واحد**: نسخة `~/Documents/mtm-upgrade-files/` **أُلغيت** (25 أغسطس 2026) | ✅ | تحقُّق قبل الحذف: صفر ملفّ حصري فيها. نسختان بلا مزامنة **تتناقضان بصمت** — والنسخة لجهاز آخر تكون `git clone` لا نسخا يدويا |
| أ‑6 | **أمر عمل الإغلاق أُدخل المستودع**: `close-out/WORK-ORDER.md` | ✅ | كان أعلى سلّم الحجّية ويُحال إليه **بلا مسار** ومن خارج git. ومعه برومت مراجعة المعمارية `MTM-Architecture-Review-Prompt.md` |

---

# الجزء ب — قبل أوّل سطر منطق عمل (ثلاث خطوات)

## ب‑1 · الهيكل الفارغ (scaffold)

**ما هو:** المجلدات والإعدادات وحدها — **صفر منطق عمل**. لا نقطة نهاية، لا شاشة، لا استعلام. الغرض أن يكون لكل ملفّ لاحق **مكان واحد بديهي**، فلا يُولد ملفّ في موضع خاطئ ثم يُنقل بعد أن صارت له إحالات.

**القرار (أتحمّله، والقيد من أمر العمل: القرارات التقنية عليّ):** يُنشأ الهيكل **كاملا** مع **اختبارات البوّابة الستّة مكتوبة وحمراء**، بلا منطق عمل. **كلفة النقض: رخيص** — حذف مجلدات فارغة.

```
mtm_app/
├─ CLAUDE.md                 ← موجود
├─ MTM-Unified-Roadmap.md    ← موجود
├─ MTM-BUILD-ORDER.md        ← هذا الملف
├─ close-out/                ← موجود (14 ملفا)
├─ apps/
│  └─ mobile/                Flutter — الهدف الأول فقط: lib/main.dart
├─ services/
│  ├─ api/                   NestJS (Modular Monolith)
│  └─ ai/                    FastAPI — تبقى فارغة حتى المرحلة 9
├─ db/
│  ├─ migrations/
│  └─ roles/                 سكربت واحد لكل دور من الستّة
├─ tests/
│  └─ gate/                  الشروط الستّة للمرحلة 0
├─ .env.example              أسماء مفاتيح `09` بلا أي قيمة
└─ .github/workflows/ci.yml
```

**ما لا يُنشأ الآن عن قصد:** `lib/main_admin.dart` ومجلد `admin/` — موضعهما **المرحلة 1.5**. ملفّ فارغ باسم هدف بناء ثانٍ هو أقرب طريق لشحنه سهوا، و`V-WEB-3` قائم أصلا ليمنع ذلك.

**فحص الخروج:** `git status` نظيف بعد لقطة واحدة، و`.env.example` فيه **صفر قيمة** وأسماء مفاتيح `09` وحدها.

## ب‑2 · اختبارات البوّابة تُكتب **قبل** ما تحرسه، وتكون **حمراء**

**لماذا بهذا الترتيب، وهو أهمّ سطر في هذا الملف:** بوّابة تُكتب بعد العمل تصير **توثيقا لما بُني**. بوّابة تُكتب قبله تصير **شرطا على ما سيُبنى**. الفرق أن الأولى تصف والثانية تمنع.

الاختبارات الستّة تفشل اليوم لأن لا قاعدة بيانات ولا أدوار. **فشلها الآن هو الصواب**، وتحوّل كل واحد منها إلى أخضر هو تعريف تقدّم المرحلة 0.

**فحص الخروج:** `npm test -- tests/gate` يخرج بستّة إخفاقات مسمّاة، لا بخطأ تجميع.

## ب‑3 · CI يشغّل الستّة على كل لقطة

**فحص الخروج:** لقطة تجريبية تُنتج تشغيلا **أحمر مرئيا**. مطوّر منفرد بلا مراجع ثانٍ ⇒ **الـ CI هو المراجع** (وهذا عين `R2`).

---

# الجزء ج — المرحلة 0: البوّابة الحاكمة · تبدأ 25 أغسطس 2026

**لا سطر من المراحل 1–10 يُبنى قبل أن تخضرّ الستّة كلها.** المرجع: `10` (المرحلة 0) و`09` §9.12. **زيادات الإغلاق: 2.5 يوم-مطوّر** — وكلفة البناء الأساسي **غير مُقدَّرة في الحزمة**، وإدراج رقم لها اختراع.

الترتيب أدناه ليس ترتيب الشروط في `10`، بل **ترتيب ما يجعلها تخضرّ**: العزل لا يُختبر قبل وجود RLS، وRLS لا تنفع قبل نزع المِلكية.

## ج‑1 · المخطط + `tenant_id` + RLS + `FORCE ROW LEVEL SECURITY`

عمود العزل الموحّد `tenant_id` في **كل** جدول تشغيلي. سياسة RLS على كل واحد، ثم `FORCE`.

**يُخضِّر الشرط (3):**
```sql
SELECT c.relname
FROM pg_class c
JOIN pg_namespace n ON n.oid = c.relnamespace
JOIN pg_attribute a ON a.attrelid = c.oid AND a.attname = 'tenant_id'
                   AND a.attnum > 0 AND NOT a.attisdropped
WHERE n.nspname = 'app' AND c.relkind = 'r'
  AND (c.relrowsecurity = false OR c.relforcerowsecurity = false);
-- المطلوب: صفر صفّ
```

## ج‑2 · الأدوار الستّة، و`app_role` لا يملك شيئا

| الدور | الخصيصة الحاسمة |
|---|---|
| `app_owner` | يملك المخطط، **ولا يخدم أي طلب** |
| `app_role` | **بلا `BYPASSRLS`، ولا يملك أي جدول** |
| `platform_ops` | `BYPASSRLS` + `default_transaction_read_only = on` |
| `platform_writer` | بلا `BYPASSRLS`، منح مُعدَّدة على **خمسة جداول حصرا** |
| `ai_reader` | views مُجمَّعة في `analytics` حصرا |
| `backup_role` | `row_security = off` كسلك تفجير |

**الفخّ الأخطر في المشروع كله هنا:** **مالك الجدول يتجاوز RLS ضمنيا، بلا منحة يمكن تفتيشها.** فلو خدم `app_role` الطلبات وهو مالك، لمرّت كل السياسات ولم تفعل شيئا — وتظهر النتيجة كتسريب صامت لا كخطأ صلاحية.

**يُخضِّر الشرط (2):**
```sql
SELECT rolbypassrls FROM pg_roles WHERE rolname = 'app_role';   -- المطلوب: false

SELECT count(*) FROM pg_class c
JOIN pg_namespace n ON n.oid = c.relnamespace
WHERE n.nspname = 'app' AND c.relowner = 'app_role'::regrole;   -- المطلوب: 0
```

## ج‑3 · تمرير سياق المستأجر بـ `SET LOCAL` عبر `nestjs-cls`

داخل معاملة الطلب حصرا. `SET LOCAL` لا `SET` — لأن PgBouncer يكسر الإعدادات المرتبطة بالجلسة (`09` مفتاح `MTM_TENANT_CONTEXT_MODE`).

**فحص الخروج:** طلب بلا سياق مستأجر **يفشل**، ولا يرجع صفوفا. الافتراض الآمن هو صفر لا الكل.

## ج‑4 · اختبارات العزل بين المستأجرين

مستأجران، بيانات لكل منهما، وقراءة بسياق الأول: **صفر صفّ للثاني**. تُشمل القراءة والكتابة والتحديث والحذف، وتُشمل `JOIN` عبر جدولين.

**يُخضِّر الشرط (1)** — وهو الشرط الذي تقوم عليه المراحل العشر كلها.

## ج‑5 · `KeyProvider` + تغليف KEK/DEK على KMS مُدار + `ReEncrypt` مرّة واحدة فعلا

**envelope encryption** لا اشتقاقا. أي ذكر HKDF-only في مستند قديم **مُبطل**: الاشتقاق يجعل حذف مستأجر واحد **مستحيلا تشفيريا**، فيسقط الـ crypto-shredding معه.

**والسبب في أن `ReEncrypt` شرط لا أثر جانبي:** **تدوير مفتاح KMS لا يُعيد تغليف الـ DEKs.** من لم ينفّذ العملية مرّة على **مفتاح اختبار** لا يعرف أن مسار التدوير موجود عنده أصلا — ويكتشف غيابه بعد أن صارت هناك بيانات حقيقية، حيث الكلفة أعلى ما تكون.

**يُخضِّر الشرط (4):** دورة تغليف/فكّ تغليف تنجح، و`ReEncrypt` نُفِّذت فعلا على مفتاح اختبار.

## ج‑6 · منح `platform_writer` — جدولا جدولا

`tenants` · `subscriptions` · `plan_limits` · `feature_flags` · `platform_access_grants`. **خمسة، لا سادس.**

**لا `ALL TABLES` ولا `ALTER DEFAULT PRIVILEGES` أبدا** ⇒ كل جدول جديد يبدأ بصفر صلاحية. وقيمة هذه القاعدة أن الإخفاق يظهر كـ **خطأ صلاحية** لا كتسريب (المخاطرة 16 في الخارطة §19: «`GRANT` واحد معقول يهدم فصل الأدوار الستّة»).

**ولماذا الدور يُنشأ الآن لا في المرحلة 1.5 حيث تُستخدمه اللوحة:** إنشاؤه لاحقا يعني **migration ثانيا يلمس المنح** — وهو بالضبط صنف التغيير الذي وُضعت هذه البوّابة لمنعه.

**يُخضِّر الشرط (5):**
```sql
SELECT DISTINCT c.relname
FROM pg_class c
JOIN pg_namespace n ON n.oid = c.relnamespace
CROSS JOIN LATERAL aclexplode(c.relacl) a
JOIN pg_roles r ON r.oid = a.grantee
WHERE n.nspname = 'app' AND c.relkind = 'r' AND r.rolname = 'platform_writer'
  AND c.relname NOT IN ('tenants','subscriptions','plan_limits',
                        'feature_flags','platform_access_grants');
-- المطلوب: صفر صفّ — وأي صفّ يُفشل البناء
```

**والاستعلام صُحّح في 26 أغسطس 2026، ولا يُعاد إلى `information_schema.role_table_grants`:**
ذلك العرض **مُصفّى بالدور المتصل** — لا يُظهر إلا منحا يكون المتصل فيها مانحا أو ممنوحا.
فلو فُحص من دور الطلب لرجع **صفر صفّ** عن منح `platform_writer`، فيقرأ الفحص **أخضر وهو
فراغ**، ويبقى نظيره («الخمسة مُمنَحة») أحمر أبدا ولو طُبّقت المنح فعلا. و`aclexplode` يقرأ
الـ ACL نفسه فلا يتأثّر بمن يسأل — فالعلّة تنعدم لا تُخفَّف. **وهذا مثال القاعدة نفسها:**
هذا الملفّ مشتقّ لا حاكم، فهو الذي يُصحَّح.

## ج‑7 · صفر ورود لـ `BLOCKED ON CO-D`

**يُخضِّر الشرط (6):** بحث نصّي على الإعداد الفعّال يرجع صفر نتيجة. `09` فيه **صفر علامة** أصلا، وهذا الفحص يمنع رجوعها.

> ## ✅ البوّابة تُعبَر عندما تخضرّ الستّة — لا قبل، ولا برأي.

---

# الجزء د — المراحل بعد البوّابة، بالترتيب

التفصيل الكامل في `10`. هنا **الترتيب والمفتاح الحاسم** لكل مرحلة فقط.

| # | المرحلة | لا تُبدأ قبل | المفتاح الحاسم | أيام (زيادات الإغلاق) |
|---|---|---|---|---|
| **1** | الهوية والمصادقة | الستّة خضراء | `access_grants` **مصدر الحقيقة لا مطالبات JWT** ⇒ الإلغاء يسري **في الطلب التالي**. و**WebAuthn الخادمي هنا** لا في 7 | 3.5 |
| **1.5** | لوحة إدارة الويب | **مفتاحا مرور مسجَّلان فعلا على جهازين** | هدف بناء ثانٍ في نفس المستودع · `admin.<domain>` والـ API تحته بوكيل عكسي ⇒ **صفر CORS** · مسارات الإدارة **غير مركّبة** على `api.<domain>` ⇒ **404 لا 403** · الثمن معلَن: `R4` | 15.5 |
| **2** | المفارز والشفتات | 1.5 | العضو **على مستوى المستأجر** ⇒ تعارض الشفتات **عبر المفارز** ممكن، ويُرفض **من قاعدة البيانات** بقيد استبعاد لا من التطبيق · قفل الحضور ثلاث طبقات | 1.0 |
| **3** | المزامنة دون اتصال | 2 | **cursor مبهم مخصّص، وPowerSync مرفوضة** · `patients.condition` **غير موجود في قاعدة البيانات المحلية** بفحص مباشر على ملف SQLCipher | 1 |
| **4** | التبديل والتغطية | 3 | `released_at` يجعل قيد الاستبعاد يسمح بإعادة الإسناد | 0 |
| **5** | المخزون | 4 · **ونتيجة `C1` معروفة** | الإدخال اليدوي **هو المسار الأساسي** والماسح تعجيل له ⇒ فشل `C1` يحذف ميزة ولا يعطّل مرحلة · المُحلِّل **fail-closed** · FEFO | 1.5 (4.5 إن نجح `C1`) |
| **6** | الورش والمرضى | 5 | مستأجر لا يفعّل واجهة المرضى **لا يُنشئ الصنف أصلا** — أرخص ضابط خصوصية هو عدم الجمع | 0 |
| **7** | التصليب الأمني | 6 | `UNEVALUATED` **ليس نجاحا وليس فشلا** · App Attest **لا يقدّم إشارة jailbreak/root/tampering — «غير معلن»** واللامتناظرية مُعلَنة | 3.5 |
| **8** | التشغيل والتعافي | 7 · **§21 مكتوب** | استعادة كاملة **نُفِّذت فعلا** وشملت فكّ عمود مشفَّر · ونسخة بدور خاطئ **تفشل بصوت عالٍ** · RPO/RTO **بمدى لا برقم مُجمَّل** | 8 |
| **9** | الذكاء الاصطناعي وSaaS | 8 | `ai_reader` **لا يستطيع بنيويا** إرجاع صفّ مستأجر — مُثبَتا باختبار يحاول ويفشل · حدّ الخطة على **الإنشاء التالي وحده، ولا يُطبَّق رجعيا** | 0 |
| **10** | ما بعد الإطلاق | 9 · بيانات تشغيلية لأسابيع | خطّ الأساس القائم على القواعد يُقاس **قبل** طبقة ML — وإلا فلا مقارنة ولا فصل تجريبي في الرسالة | مؤجَّلات |

**الملتزَم به: 36.5 يوما-مطوّر** — و**39.5** إن نجح `C1`. والمؤجَّلات الخمسة (17.5 يوما) **لا تُجمع** لأنها تُفتح بمُشغِّلات رقمية لا بعبور مرحلة.

---

# الجزء هـ — عملان يبدآن بالتوازي، ولا يحجزان البوّابة

هذان **الحاجزان غير التقنيّين الوحيدان**، وكلاهما **عمل يُنجَز لا قرار يُنتظر**.

**`C1` — قياس انتشار DataMatrix ميدانيا.** البروتوكول في `06`: n=50، عيّنة طبقية، والنجاح = **≥70% من العشرين الأكثر صرفا تُنتج التشغيلة والصلاحية معا**. يحجز **مسار الماسح في المرحلة 5 وحده**. يُنفَّذ في أي وقت قبلها. **ولا رقم مقدَّر له في أي ملفّ، ولا يُقدَّر** — هذا قياس، والتقدير يُفسده.

**`G4` — §21 وقالب DPA وقالب إشعار الخرق.** يحجز **أول مستأجر مدفوع** لا البناء. يومان داخل المرحلة 8، ويجوز تقديمهما.

---

# الجزء و — المؤجَّلات الخمسة: الرقم، والمكان الذي يُقرأ فيه

**لا مؤجَّل بلا رقم، ولا رقم بلا موضع يُفحص فيه.**

| # | ما هو | المُشغِّل | يُفحص | الكلفة |
|---|---|---|---|---|
| `L1` | WebAuthn/Passkeys للمستأجرين | **100 مستأجر** | أول كل شهر | 4 |
| `L2` | بوّابة دفع آلية | **>40 حدث تفعيل/تجديد يدوي في شهر تقويمي** (لا عدد المستأجرين — مزيج الخطط يفصل العمل عن العدد) | أول كل شهر مع `L1` | 6 |
| `L3` | لوحة تحكّم المنصّة | **استُهلك — صار المرحلة 1.5.** والباقي منه عدّاد **15 حدثا شهريا** على `platform_access_grants` | شهريا | — |
| `L4` | ختم زمني خارجي (RFC 3161) | **150 مستأجرا مدفوعا** أو **أول طلب مكتوب** من مستأجر لسلسلة قابلة للإثبات لطرف ثالث | شهريا مع `L1`/`L2` | 2 |
| `L5` | كشف تعارض ورشة/شفت | **متوسط 4 ورش شهريا لمستأجر واحد على 3 أشهر متصلة** أو **3 بلاغات موثَّقة** | **1 يناير · 1 أبريل · 1 يوليو · 1 أكتوبر** | 1.5 |

---

# الجزء ز — أربع قواعد تُخالف الحدس، فتُكرَّر هنا

القيود **التسعة** كلها في `CLAUDE.md` §2 ولا تُعاد. هذه الأربع تُعاد لأن **الحدس السليم يقود إلى عكسها**:

**`R1`–`R4` تُعلَن ولا تُغلق.** ضابط جديد يبدو كافيا لا يحذف صفّ مخاطرة. الرسالة تُقيَّم على صدق الإعلان، والمنتج يُشترى على أساسه.

**مطوّر منفرد، وهذا قيد على الحلول لا على الطموح.** لا مراجعة زميل، لا on-call، لا فصل واجبات بشريا. الحلّ المقبول **آلي** أو **مُعلَن كخطر** — وهو `R2` بعينه.

**«غير معلن» و«غير قابل للتحقّق» أوسمة نهائية لا مؤقّتة** (`FLAG-4`). لا تُستبدل برابط لم يُفتح. **صفر رابط في حزمة الإغلاق، ويبقى صفرا.**

**`D1`–`D58` و`CO-D1`–`CO-D5` رمزان مختلفان بنفس الأسماء.** `D3` ليس `CO-D3`. تُكتب البادئة `CO-D` كاملة دائما — والخلط يُنتج إحالة **تبدو صحيحة** وتشير إلى قرار آخر، وهو أسوأ من إحالة مكسورة.

---

# سجل التقدّم

| | الخطوة | الحالة | التاريخ |
|---|---|---|---|
| أ | الأساسات الخمسة (المسار، git، الأسرار، `CLAUDE.md`، نسخة القراءة) | ✅ | 25 أغسطس 2026 |
| ب‑1 | الهيكل الفارغ | ✅ | 26 أغسطس 2026 |
| ب‑2 | اختبارات البوّابة الستّة مكتوبة و**حمراء** | ✍ | 26 أغسطس 2026 |
| ب‑3 | CI يشغّلها على كل لقطة | ✍ | 26 أغسطس 2026 |
| ج‑1 | RLS + `FORCE` على كل جدول حامل `tenant_id` | ✍ | 26 أغسطس 2026 |
| ج‑2 | الأدوار الستّة، و`app_role` بلا مِلكية ولا `BYPASSRLS` | ✍ | 26 أغسطس 2026 |
| ج‑3 | `SET LOCAL` عبر `nestjs-cls` | ☐ | |
| ج‑4 | اختبارات العزل بين المستأجرين | ✍ | 26 أغسطس 2026 |
| ج‑5 | KEK/DEK + `ReEncrypt` على مفتاح اختبار | ✍ | 26 أغسطس 2026 |
| ج‑6 | `platform_writer` على الخمسة حصرا | ✍ | 26 أغسطس 2026 |
| ج‑7 | صفر `BLOCKED ON CO-D` | ✍ | 26 أغسطس 2026 |
| **البوّابة** | **الستّة خضراء ⇒ المرحلة 0 عُبرت** | ☐ | |

**رمز الحالة `✍` أُدخل في 26 أغسطس 2026، ومعناه دقيق: «مكتوب، وفحص خروجه لم يُشغَّل».** و`☐` تعني «لم يُكتب». والتمييز بينهما ضابط لا زخرفة: `☐` واحدة تُقرأ «لا شيء هنا» فيُعاد كتابة ما هو مكتوب، أو تُقرأ «تمّ» فيُبنى فوق غير مُثبَت — وهو عين العطب الذي في `CLAUDE.md` نفسها. **ولا يُرقّى `✍` إلى `✅` إلا بمخرَج تشغيل مقروء**، لا برأي ولا بمراجعة نظر.

**وما يُرقّي كل `✍`:** ب‑2 و**ج‑4** بـ `npm test -- tests/gate` (ستّة إخفاقات مسمّاة لا خطأ تجميع) · ب‑3 بلقطة تجريبية تُنتج تشغيلا **أحمر مرئيا** على GitHub · ج‑1 وج‑2 وج‑6 بتشغيل `db/roles/*.sql` ثم `db/migrations/0001` ثم `0002` على PostgreSQL 13+ · ج‑5 بإقرار المزوّد وتسجيل محوّله (`registerAdapter`) ثم `ReEncrypt` على **مفتاح اختبار** · ج‑7 بوجود إعداد فعّال محقون يُفحَص أصلا.

**وج‑3 وحدها `☐` بحقّ:** موضعها الخادم (`nestjs-cls` داخل معاملة الطلب)، ولا خادم بعد. **ونصف عقدها منفَّذ الآن في قاعدة البيانات لا في التطبيق:** `app.current_tenant_id()` في `0001` **ترفع `42501`** عند غياب السياق، فطلب بلا سياق يفشل ولو نُسي الحرس في الشيفرة — والافتراض الآمن صفر لا الكل، بنيويا.

**بند مفتوح مُعلَن (26 أغسطس 2026) — يُغلق قبل ب‑2 وج‑1:** `mobile-threat-model` و`masvs-checklist` **لم تُشغَّلا** قبل ب‑1. سببه أنّهما `disable-model-invocation: true` فلا تُطلقان إلا بـ `/mobile-threat-model` و`/masvs-checklist` من أحمد نفسه. وأُكمل ب‑1 دونهما لأنه **هيكل بصفر منطق وصفر قرار أمني** (الأدوار وRLS والمفاتيح كلّها في ج‑1…ج‑7) وكلفة نقضه رخيصة. و**يُعلَن هنا لأنّ `CLAUDE.md` §6 يقول إنّ إهمالهما «لا يُعلن نفسه»** — فالإعلان هو الضابط. **ولا يُكتب سطر شيفرة في ب‑2 ولا ج‑1 قبل تشغيلهما.**
